# 宠物社交微信小程序技术文档

## 1. 项目目标

使用以下技术栈开发一个完整的宠物社交微信小程序：

前端：

```text
Taro + React + TypeScript + NutUI/Taro + Zustand
```

后端：

```text
Go + Gin + GORM + MySQL 8 + Redis + JWT
```

管理后台：

```text
React + Vite + TypeScript + Ant Design Pro
```

存储与第三方服务：

```text
腾讯云 COS 或阿里云 OSS
微信小程序服务端 API
微信内容安全接口
腾讯地图 API
```

---

# 一、整体架构

## 1.1 架构图

```text
微信小程序前端
    |
    | HTTPS
    |
Nginx
    |
Go Gin API 服务
    |
------------------------------------------------
| MySQL | Redis | COS/OSS | 微信 API | 地图 API |
------------------------------------------------
    |
管理后台 Web
```

## 1.2 服务职责

Go 后端负责：

1. 微信登录。
2. JWT 鉴权。
3. 用户管理。
4. 宠物管理。
5. 位置保存。
6. 附近宠物查询。
7. 邀请业务。
8. 动态业务。
9. 点赞评论。
10. 举报拉黑。
11. 文件上传凭证。
12. 内容安全审核。
13. 后台管理接口。

前端负责：

1. 小程序页面渲染。
2. 用户交互。
3. wx.login。
4. 位置授权。
5. 图片选择和上传。
6. 请求后端 API。
7. 本地状态管理。

---

# 二、代码仓库结构

建议使用 monorepo：

```text
pet-social/
├── backend/
│   └── pet-social-api/
├── frontend/
│   └── pet-social-mini/
├── admin/
│   └── pet-social-admin/
├── docs/
│   ├── prd.md
│   ├── api.md
│   ├── db.md
│   └── prototype-prompt.md
├── docker-compose.yml
└── README.md
```

---

# 三、后端目录结构

```text
backend/pet-social-api/
├── cmd/
│   └── server/
│       └── main.go
├── internal/
│   ├── config/
│   │   └── config.go
│   ├── router/
│   │   └── router.go
│   ├── middleware/
│   │   ├── auth.go
│   │   ├── cors.go
│   │   ├── logger.go
│   │   ├── recovery.go
│   │   └── rate_limit.go
│   ├── controller/
│   │   ├── auth_controller.go
│   │   ├── user_controller.go
│   │   ├── pet_controller.go
│   │   ├── location_controller.go
│   │   ├── nearby_controller.go
│   │   ├── invite_controller.go
│   │   ├── post_controller.go
│   │   ├── comment_controller.go
│   │   ├── like_controller.go
│   │   ├── report_controller.go
│   │   ├── block_controller.go
│   │   └── admin_controller.go
│   ├── service/
│   │   ├── auth_service.go
│   │   ├── pet_service.go
│   │   ├── nearby_service.go
│   │   ├── invite_service.go
│   │   ├── post_service.go
│   │   ├── comment_service.go
│   │   ├── report_service.go
│   │   └── upload_service.go
│   ├── repository/
│   │   ├── user_repository.go
│   │   ├── pet_repository.go
│   │   ├── location_repository.go
│   │   ├── invite_repository.go
│   │   ├── post_repository.go
│   │   ├── comment_repository.go
│   │   └── report_repository.go
│   ├── model/
│   │   ├── user.go
│   │   ├── pet.go
│   │   ├── user_location.go
│   │   ├── invite.go
│   │   ├── post.go
│   │   ├── comment.go
│   │   ├── like.go
│   │   ├── follow.go
│   │   ├── block.go
│   │   ├── report.go
│   │   └── notification.go
│   ├── dto/
│   │   ├── request/
│   │   └── response/
│   ├── response/
│   │   └── response.go
│   ├── validator/
│   └── task/
│       └── invite_expire_task.go
├── pkg/
│   ├── jwt/
│   ├── redis/
│   ├── logger/
│   ├── storage/
│   ├── wechat/
│   ├── map/
│   └── security/
├── migrations/
├── Dockerfile
├── go.mod
└── go.sum
```

---

# 四、后端配置

## 4.1 环境变量

```env
APP_ENV=dev
APP_PORT=8080

MYSQL_DSN=root:password@tcp(127.0.0.1:3306)/pet_social?charset=utf8mb4&parseTime=True&loc=Local
REDIS_ADDR=127.0.0.1:6379
REDIS_PASSWORD=
REDIS_DB=0

JWT_SECRET=change_me
JWT_EXPIRE_HOURS=168

WECHAT_APP_ID=your_app_id
WECHAT_APP_SECRET=your_app_secret

COS_SECRET_ID=your_secret_id
COS_SECRET_KEY=your_secret_key
COS_BUCKET=your_bucket
COS_REGION=ap-shanghai

TENCENT_MAP_KEY=your_map_key
```

## 4.2 安全要求

1. 不要把 WECHAT_APP_SECRET 放到前端。
2. 不要把 WECHAT_APP_SECRET 提交到 Git。
3. 不要在日志中打印 openid、session_key、AppSecret。
4. 生产环境必须使用 HTTPS。
5. 生产环境 JWT_SECRET 必须随机生成。

---

# 五、数据库设计

## 5.1 users

```sql
CREATE TABLE users (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    openid VARCHAR(128) NOT NULL UNIQUE,
    unionid VARCHAR(128) DEFAULT NULL,
    nickname VARCHAR(64) DEFAULT '',
    avatar_url VARCHAR(512) DEFAULT '',
    phone VARCHAR(32) DEFAULT '',
    gender VARCHAR(16) DEFAULT 'unknown',
    city VARCHAR(64) DEFAULT '',
    status VARCHAR(32) DEFAULT 'normal',
    allow_nearby_visible TINYINT DEFAULT 1,
    allow_stranger_invite TINYINT DEFAULT 1,
    allow_comment TINYINT DEFAULT 1,
    show_owner_name TINYINT DEFAULT 1,
    show_city TINYINT DEFAULT 1,
    notification_enabled TINYINT DEFAULT 1,
    last_login_at DATETIME DEFAULT NULL,
    created_at DATETIME NOT NULL,
    updated_at DATETIME NOT NULL,
    deleted_at DATETIME DEFAULT NULL,
    INDEX idx_openid (openid),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

## 5.2 pets

```sql
CREATE TABLE pets (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    name VARCHAR(64) NOT NULL,
    avatar_url VARCHAR(512) DEFAULT '',
    type VARCHAR(32) NOT NULL,
    breed VARCHAR(64) DEFAULT '',
    gender VARCHAR(16) DEFAULT 'unknown',
    birthday DATE DEFAULT NULL,
    weight DECIMAL(5,2) DEFAULT NULL,
    sterilized TINYINT DEFAULT 0,
    vaccine_status VARCHAR(32) DEFAULT 'unknown',
    personality_tags JSON DEFAULT NULL,
    interest_tags JSON DEFAULT NULL,
    description VARCHAR(500) DEFAULT '',
    is_default TINYINT DEFAULT 0,
    visible TINYINT DEFAULT 1,
    status VARCHAR(32) DEFAULT 'normal',
    created_at DATETIME NOT NULL,
    updated_at DATETIME NOT NULL,
    deleted_at DATETIME DEFAULT NULL,
    INDEX idx_user_id (user_id),
    INDEX idx_type (type),
    INDEX idx_visible (visible),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

## 5.3 user_locations

```sql
CREATE TABLE user_locations (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL UNIQUE,
    latitude DECIMAL(10,7) NOT NULL,
    longitude DECIMAL(10,7) NOT NULL,
    geohash VARCHAR(32) DEFAULT '',
    city VARCHAR(64) DEFAULT '',
    district VARCHAR(64) DEFAULT '',
    visible TINYINT DEFAULT 1,
    updated_at DATETIME NOT NULL,
    INDEX idx_user_id (user_id),
    INDEX idx_geohash (geohash),
    INDEX idx_visible (visible)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

## 5.4 invites

```sql
CREATE TABLE invites (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    from_user_id BIGINT NOT NULL,
    from_pet_id BIGINT NOT NULL,
    to_user_id BIGINT NOT NULL,
    to_pet_id BIGINT NOT NULL,
    type VARCHAR(32) NOT NULL,
    title VARCHAR(100) NOT NULL,
    description VARCHAR(500) DEFAULT '',
    location_name VARCHAR(128) DEFAULT '',
    meet_time DATETIME DEFAULT NULL,
    status VARCHAR(32) NOT NULL DEFAULT 'pending',
    created_at DATETIME NOT NULL,
    updated_at DATETIME NOT NULL,
    INDEX idx_from_user (from_user_id),
    INDEX idx_to_user (to_user_id),
    INDEX idx_status (status),
    INDEX idx_meet_time (meet_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

## 5.5 posts

```sql
CREATE TABLE posts (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    pet_id BIGINT NOT NULL,
    content TEXT,
    images JSON DEFAULT NULL,
    location_name VARCHAR(128) DEFAULT '',
    city VARCHAR(64) DEFAULT '',
    topic_tags JSON DEFAULT NULL,
    visibility VARCHAR(32) DEFAULT 'public',
    like_count INT DEFAULT 0,
    comment_count INT DEFAULT 0,
    status VARCHAR(32) DEFAULT 'normal',
    created_at DATETIME NOT NULL,
    updated_at DATETIME NOT NULL,
    deleted_at DATETIME DEFAULT NULL,
    INDEX idx_user_id (user_id),
    INDEX idx_pet_id (pet_id),
    INDEX idx_city (city),
    INDEX idx_created_at (created_at),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

## 5.6 comments

```sql
CREATE TABLE comments (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    post_id BIGINT NOT NULL,
    user_id BIGINT NOT NULL,
    pet_id BIGINT DEFAULT NULL,
    parent_id BIGINT DEFAULT 0,
    content VARCHAR(500) NOT NULL,
    status VARCHAR(32) DEFAULT 'normal',
    created_at DATETIME NOT NULL,
    updated_at DATETIME NOT NULL,
    deleted_at DATETIME DEFAULT NULL,
    INDEX idx_post_id (post_id),
    INDEX idx_user_id (user_id),
    INDEX idx_parent_id (parent_id),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

## 5.7 likes

```sql
CREATE TABLE likes (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    target_type VARCHAR(32) NOT NULL,
    target_id BIGINT NOT NULL,
    created_at DATETIME NOT NULL,
    UNIQUE KEY uk_user_target (user_id, target_type, target_id),
    INDEX idx_target (target_type, target_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

## 5.8 follows

```sql
CREATE TABLE follows (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    pet_id BIGINT NOT NULL,
    created_at DATETIME NOT NULL,
    UNIQUE KEY uk_user_pet (user_id, pet_id),
    INDEX idx_pet_id (pet_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

## 5.9 blocks

```sql
CREATE TABLE blocks (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    blocked_user_id BIGINT NOT NULL,
    reason VARCHAR(255) DEFAULT '',
    created_at DATETIME NOT NULL,
    UNIQUE KEY uk_block (user_id, blocked_user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

## 5.10 reports

```sql
CREATE TABLE reports (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    reporter_user_id BIGINT NOT NULL,
    target_type VARCHAR(32) NOT NULL,
    target_id BIGINT NOT NULL,
    reason VARCHAR(64) NOT NULL,
    description VARCHAR(500) DEFAULT '',
    images JSON DEFAULT NULL,
    status VARCHAR(32) DEFAULT 'pending',
    handled_by BIGINT DEFAULT NULL,
    handle_note VARCHAR(500) DEFAULT '',
    handled_at DATETIME DEFAULT NULL,
    created_at DATETIME NOT NULL,
    INDEX idx_target (target_type, target_id),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

## 5.11 notifications

```sql
CREATE TABLE notifications (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    type VARCHAR(32) NOT NULL,
    title VARCHAR(100) NOT NULL,
    content VARCHAR(500) DEFAULT '',
    data JSON DEFAULT NULL,
    is_read TINYINT DEFAULT 0,
    created_at DATETIME NOT NULL,
    INDEX idx_user_read (user_id, is_read),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

---

# 六、API 规范

## 6.1 请求 Header

```http
Authorization: Bearer <token>
Content-Type: application/json
```

## 6.2 通用成功响应

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

## 6.3 通用错误响应

```json
{
  "code": 40001,
  "message": "token expired",
  "data": null
}
```

## 6.4 分页响应

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [],
    "page": 1,
    "page_size": 20,
    "total": 0
  }
}
```

## 6.5 错误码

```text
0：成功
40000：请求参数错误
40001：Token 无效或过期
40003：无权限
40004：资源不存在
40009：操作冲突
40029：请求频率过高
50000：服务器错误
```

---

# 七、核心接口

## 7.1 微信登录

```http
POST /api/v1/auth/wechat-login
```

请求：

```json
{
  "code": "wx_login_code",
  "nickname": "小V",
  "avatar_url": "https://example.com/avatar.png"
}
```

响应：

```json
{
  "token": "jwt_token",
  "user": {
    "id": 1,
    "nickname": "小V",
    "avatar_url": ""
  },
  "has_pet": true
}
```

---

## 7.2 获取当前用户

```http
GET /api/v1/user/me
```

响应：

```json
{
  "id": 1,
  "nickname": "小V",
  "avatar_url": "",
  "city": "上海",
  "allow_nearby_visible": true,
  "allow_stranger_invite": true
}
```

---

## 7.3 创建宠物

```http
POST /api/v1/pets
```

请求：

```json
{
  "name": "豆包",
  "type": "dog",
  "breed": "柯基",
  "gender": "male",
  "birthday": "2022-01-01",
  "weight": 12.5,
  "sterilized": true,
  "vaccine_status": "completed",
  "personality_tags": ["活泼", "亲人"],
  "interest_tags": ["遛弯", "飞盘"],
  "description": "喜欢交朋友的小柯基"
}
```

---

## 7.4 我的宠物列表

```http
GET /api/v1/pets/my
```

---

## 7.5 宠物详情

```http
GET /api/v1/pets/:id
```

---

## 7.6 更新宠物

```http
PUT /api/v1/pets/:id
```

---

## 7.7 删除宠物

```http
DELETE /api/v1/pets/:id
```

---

## 7.8 设置默认宠物

```http
POST /api/v1/pets/:id/default
```

---

## 7.9 更新位置

```http
POST /api/v1/location/update
```

请求：

```json
{
  "latitude": 31.2304,
  "longitude": 121.4737,
  "city": "上海",
  "district": "徐汇区"
}
```

---

## 7.10 附近宠物列表

```http
GET /api/v1/nearby/pets?distance=3000&type=dog&gender=male&page=1&page_size=20
```

响应：

```json
{
  "list": [
    {
      "pet_id": 1,
      "name": "豆包",
      "avatar_url": "",
      "type": "dog",
      "breed": "柯基",
      "age_text": "2岁",
      "gender": "male",
      "personality_tags": ["活泼", "亲人"],
      "interest_tags": ["遛弯", "飞盘"],
      "distance_text": "1km 内",
      "active_text": "10分钟前在线",
      "can_invite": true
    }
  ],
  "page": 1,
  "page_size": 20,
  "total": 1
}
```

注意：不要返回其他用户的真实 latitude / longitude。

---

## 7.11 发起邀请

```http
POST /api/v1/invites
```

请求：

```json
{
  "from_pet_id": 1,
  "to_pet_id": 2,
  "type": "walk",
  "title": "一起遛弯吗？",
  "description": "今晚 7 点附近公园散步",
  "location_name": "中央公园",
  "meet_time": "2026-06-10 19:00:00"
}
```

---

## 7.12 邀请列表

```http
GET /api/v1/invites?box=received&status=pending&page=1&page_size=20
```

box 支持：

```text
received
sent
```

---

## 7.13 邀请详情

```http
GET /api/v1/invites/:id
```

---

## 7.14 接受邀请

```http
POST /api/v1/invites/:id/accept
```

---

## 7.15 拒绝邀请

```http
POST /api/v1/invites/:id/reject
```

---

## 7.16 取消邀请

```http
POST /api/v1/invites/:id/cancel
```

---

## 7.17 发布动态

```http
POST /api/v1/posts
```

请求：

```json
{
  "pet_id": 1,
  "content": "今天豆包第一次去公园，超级开心！",
  "images": ["https://cdn.example.com/1.jpg"],
  "location_name": "中央公园",
  "topic_tags": ["遛弯", "柯基"],
  "visibility": "nearby"
}
```

---

## 7.18 动态列表

```http
GET /api/v1/posts?feed=nearby&page=1&page_size=20
```

feed 支持：

```text
recommend
nearby
following
mine
```

---

## 7.19 动态详情

```http
GET /api/v1/posts/:id
```

---

## 7.20 删除动态

```http
DELETE /api/v1/posts/:id
```

---

## 7.21 点赞动态

```http
POST /api/v1/posts/:id/like
```

---

## 7.22 取消点赞

```http
DELETE /api/v1/posts/:id/like
```

---

## 7.23 评论动态

```http
POST /api/v1/posts/:id/comments
```

请求：

```json
{
  "content": "太可爱了！"
}
```

---

## 7.24 评论列表

```http
GET /api/v1/posts/:id/comments?page=1&page_size=20
```

---

## 7.25 删除评论

```http
DELETE /api/v1/comments/:id
```

---

## 7.26 关注宠物

```http
POST /api/v1/pets/:id/follow
```

---

## 7.27 取消关注宠物

```http
DELETE /api/v1/pets/:id/follow
```

---

## 7.28 举报

```http
POST /api/v1/reports
```

请求：

```json
{
  "target_type": "post",
  "target_id": 1001,
  "reason": "广告营销",
  "description": "疑似营销广告",
  "images": []
}
```

---

## 7.29 拉黑

```http
POST /api/v1/blocks
```

请求：

```json
{
  "blocked_user_id": 2,
  "reason": "骚扰"
}
```

---

## 7.30 隐私设置

```http
PUT /api/v1/user/privacy
```

请求：

```json
{
  "allow_nearby_visible": true,
  "allow_stranger_invite": true,
  "allow_comment": true,
  "show_owner_name": true,
  "show_city": true,
  "notification_enabled": true
}
```

---

# 八、Gin 路由示例

```go
func RegisterRoutes(r *gin.Engine) {
    api := r.Group("/api/v1")

    api.POST("/auth/wechat-login", authController.WechatLogin)

    auth := api.Group("")
    auth.Use(middleware.JWTAuth())

    auth.GET("/user/me", userController.Me)
    auth.PUT("/user/privacy", userController.UpdatePrivacy)

    auth.POST("/pets", petController.Create)
    auth.GET("/pets/my", petController.MyPets)
    auth.GET("/pets/:id", petController.Detail)
    auth.PUT("/pets/:id", petController.Update)
    auth.DELETE("/pets/:id", petController.Delete)
    auth.POST("/pets/:id/default", petController.SetDefault)
    auth.POST("/pets/:id/follow", petController.Follow)
    auth.DELETE("/pets/:id/follow", petController.Unfollow)

    auth.POST("/location/update", locationController.Update)
    auth.GET("/nearby/pets", nearbyController.Pets)

    auth.POST("/invites", inviteController.Create)
    auth.GET("/invites", inviteController.List)
    auth.GET("/invites/:id", inviteController.Detail)
    auth.POST("/invites/:id/accept", inviteController.Accept)
    auth.POST("/invites/:id/reject", inviteController.Reject)
    auth.POST("/invites/:id/cancel", inviteController.Cancel)

    auth.POST("/posts", postController.Create)
    auth.GET("/posts", postController.List)
    auth.GET("/posts/:id", postController.Detail)
    auth.DELETE("/posts/:id", postController.Delete)
    auth.POST("/posts/:id/like", postController.Like)
    auth.DELETE("/posts/:id/like", postController.Unlike)
    auth.POST("/posts/:id/comments", commentController.Create)
    auth.GET("/posts/:id/comments", commentController.List)
    auth.DELETE("/comments/:id", commentController.Delete)

    auth.POST("/reports", reportController.Create)
    auth.POST("/blocks", blockController.Create)
}
```

---

# 九、附近距离计算

## 9.1 MVP 方案

MVP 使用 MySQL Haversine 公式计算距离。

```sql
SELECT
    ul.user_id,
    (
      6371000 * acos(
        cos(radians(?)) * cos(radians(ul.latitude)) *
        cos(radians(ul.longitude) - radians(?)) +
        sin(radians(?)) * sin(radians(ul.latitude))
      )
    ) AS distance
FROM user_locations ul
WHERE ul.visible = 1
HAVING distance < ?
ORDER BY distance ASC
LIMIT ?, ?;
```

## 9.2 查询逻辑

NearbyPets 查询逻辑：

1. 获取当前用户位置。
2. 查询 user_locations 中 visible = 1 的用户。
3. 排除当前用户。
4. 排除互相拉黑用户。
5. 关联 users，要求 user.status = normal。
6. 关联 pets，要求 pet.visible = 1 且 pet.status = normal。
7. 根据筛选条件过滤 type、gender 等。
8. 计算距离。
9. 返回模糊距离文本。
10. 不返回真实经纬度。

## 9.3 距离文本生成

```text
distance <= 500：500m 内
distance <= 1000：1km 内
distance <= 3000：3km 内
distance <= 5000：5km 内
其他：10km 内
```

## 9.4 后续优化

用户增长后升级：

```text
1 万用户以内：MySQL Haversine
10 万用户以内：Redis GEO
更大规模：PostGIS / Elasticsearch
```

---

# 十、业务规则

## 10.1 创建宠物规则

1. name 必填，最多 64 字。
2. type 必填。
3. description 最多 500 字。
4. personality_tags 最多 10 个。
5. interest_tags 最多 10 个。
6. 第一只宠物自动 is_default = 1。
7. 同一个用户可以创建多只宠物。

## 10.2 发起邀请规则

CreateInvite 必须检查：

1. from_pet_id 属于当前用户。
2. to_pet_id 存在。
3. to_pet_id 不属于当前用户。
4. 对方用户状态正常。
5. 对方允许陌生邀请。
6. 双方没有拉黑关系。
7. 当前用户当日邀请次数未超过限制。
8. 同一 to_pet_id 24 小时内没有重复 pending 邀请。
9. meet_time 不能早于当前时间。

## 10.3 接受邀请规则

AcceptInvite 必须检查：

1. 邀请存在。
2. 当前用户是 to_user_id。
3. 邀请状态是 pending。
4. 邀请未过期。
5. 更新状态为 accepted。
6. 给发起人创建通知。

## 10.4 拒绝邀请规则

RejectInvite 必须检查：

1. 邀请存在。
2. 当前用户是 to_user_id。
3. 邀请状态是 pending。
4. 更新状态为 rejected。
5. 给发起人创建通知。

## 10.5 取消邀请规则

CancelInvite 必须检查：

1. 邀请存在。
2. 当前用户是 from_user_id。
3. 邀请状态是 pending。
4. 更新状态为 cancelled。
5. 给接收人创建通知。

## 10.6 发布动态规则

1. pet_id 必须属于当前用户。
2. content 和 images 至少有一个不为空。
3. content 最多 1000 字。
4. images 最多 9 张。
5. visibility 必须是合法枚举。
6. 文本需要审核。
7. 图片需要审核。
8. 发布成功后进入动态列表。

## 10.7 点赞规则

1. 一个用户对同一 post 只能点赞一次。
2. 点赞时插入 likes。
3. post.like_count + 1。
4. 取消点赞时删除 likes。
5. post.like_count - 1。
6. 需要使用事务。

## 10.8 评论规则

1. 评论内容必填。
2. 最多 500 字。
3. 文本需要审核。
4. 评论成功后 post.comment_count + 1。
5. 删除评论后 post.comment_count - 1。
6. 需要使用事务。

---

# 十一、Redis 设计

## 11.1 使用场景

1. 登录 token 黑名单。
2. 邀请频率限制。
3. 评论频率限制。
4. 附近查询缓存。
5. 未读消息数。
6. 防重复提交。
7. 微信 session 缓存。

## 11.2 Key 设计

```text
auth:blacklist:{token}
wechat:session:{user_id}
limit:invite:{user_id}:{yyyyMMdd}
limit:comment:{user_id}:{yyyyMMddHHmm}
nearby:pets:{geohash}:{filter_hash}
notification:unread:{user_id}
lock:submit:{user_id}:{action}
```

---

# 十二、内容安全

## 12.1 审核对象

需要审核：

1. 宠物昵称。
2. 宠物简介。
3. 动态文字。
4. 动态图片。
5. 评论文字。
6. 邀请说明。
7. 举报说明。

## 12.2 审核策略

MVP：

1. 文本同步审核。
2. 图片异步审核。
3. 审核失败时隐藏内容。
4. 后台人工复核。
5. 多次违规用户限制发布和邀请。

---

# 十三、文件上传

## 13.1 上传流程

```text
前端选择图片
前端请求后端获取上传凭证
后端返回 COS/OSS 临时凭证
前端直传对象存储
前端拿到图片 URL
前端提交业务接口
后端保存 URL
后端异步审核图片
```

## 13.2 限制

1. 单张图片最大 5MB。
2. 动态最多 9 张。
3. 宠物头像建议压缩到 500KB 内。
4. 只允许 jpg、jpeg、png、webp。
5. 禁止上传可执行文件。

---

# 十四、前端项目结构

```text
frontend/pet-social-mini/
├── src/
│   ├── app.config.ts
│   ├── app.tsx
│   ├── pages/
│   │   ├── login/
│   │   ├── nearby/
│   │   ├── feed/
│   │   ├── invite/
│   │   └── mine/
│   ├── subpackages/
│   │   ├── pet/
│   │   │   ├── create/
│   │   │   ├── edit/
│   │   │   └── detail/
│   │   ├── post/
│   │   │   ├── create/
│   │   │   └── detail/
│   │   └── settings/
│   │       ├── privacy/
│   │       ├── block/
│   │       └── report/
│   ├── components/
│   │   ├── PetCard/
│   │   ├── PostCard/
│   │   ├── InviteCard/
│   │   ├── TagList/
│   │   └── EmptyState/
│   ├── services/
│   │   ├── request.ts
│   │   ├── auth.ts
│   │   ├── pet.ts
│   │   ├── nearby.ts
│   │   ├── invite.ts
│   │   ├── post.ts
│   │   └── user.ts
│   ├── store/
│   │   ├── userStore.ts
│   │   ├── petStore.ts
│   │   ├── locationStore.ts
│   │   └── notificationStore.ts
│   ├── hooks/
│   ├── utils/
│   ├── constants/
│   └── assets/
├── package.json
└── tsconfig.json
```

## 14.1 前端状态

需要 store：

1. userStore：token、用户信息、登录状态。
2. petStore：我的宠物、当前宠物。
3. locationStore：位置授权状态、当前城市。
4. notificationStore：未读消息数。
5. filterStore：附近筛选条件。

## 14.2 request 封装要求

request.ts 必须实现：

1. baseURL 配置。
2. 自动携带 token。
3. 统一处理 401。
4. 统一处理错误 toast。
5. 支持 GET/POST/PUT/DELETE。
6. 支持分页参数。
7. 防重复提交，可选。

---

# 十五、管理后台

## 15.1 技术栈

```text
React + Vite + TypeScript + Ant Design Pro
```

## 15.2 页面

```text
/admin/login
/admin/dashboard
/admin/users
/admin/pets
/admin/posts
/admin/comments
/admin/invites
/admin/reports
/admin/announcements
/admin/admins
```

## 15.3 管理后台权限

MVP 可以先做简单管理员角色：

```text
super_admin
auditor
operator
```

---

# 十六、Docker Compose

```yaml
version: "3.8"

services:
  api:
    build: ./backend/pet-social-api
    container_name: pet-social-api
    ports:
      - "8080:8080"
    environment:
      - APP_ENV=dev
      - APP_PORT=8080
      - MYSQL_DSN=root:password@tcp(mysql:3306)/pet_social?charset=utf8mb4&parseTime=True&loc=Local
      - REDIS_ADDR=redis:6379
      - JWT_SECRET=change_me
    depends_on:
      - mysql
      - redis

  mysql:
    image: mysql:8
    container_name: pet-social-mysql
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: pet_social
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  redis:
    image: redis:7
    container_name: pet-social-redis
    ports:
      - "6379:6379"

volumes:
  mysql_data:
```

---

# 十七、Codex 开发顺序

请 Codex 按以下顺序开发：

## 阶段 1：后端基础

1. 初始化 Go module。
2. 集成 Gin。
3. 集成 GORM。
4. 连接 MySQL。
5. 连接 Redis。
6. 实现统一 response。
7. 实现 JWT。
8. 实现 middleware。
9. 实现 migrations。

## 阶段 2：登录与用户

1. 实现微信登录接口。
2. 实现用户表。
3. 实现 token 签发。
4. 实现获取当前用户接口。
5. 实现隐私设置接口。

## 阶段 3：宠物

1. 实现宠物 model。
2. 实现创建宠物。
3. 实现我的宠物列表。
4. 实现宠物详情。
5. 实现编辑宠物。
6. 实现删除宠物。
7. 实现默认宠物。

## 阶段 4：附近

1. 实现更新位置。
2. 实现附近宠物查询。
3. 实现距离计算。
4. 实现筛选。
5. 实现隐私过滤。
6. 确保不返回真实经纬度。

## 阶段 5：邀请

1. 实现发起邀请。
2. 实现邀请列表。
3. 实现邀请详情。
4. 实现接受邀请。
5. 实现拒绝邀请。
6. 实现取消邀请。
7. 实现邀请限制。
8. 实现邀请通知。

## 阶段 6：动态

1. 实现发布动态。
2. 实现动态列表。
3. 实现动态详情。
4. 实现删除动态。
5. 实现点赞。
6. 实现取消点赞。
7. 实现评论。
8. 实现评论列表。
9. 实现删除评论。

## 阶段 7：举报拉黑

1. 实现举报。
2. 实现拉黑。
3. 在附近、宠物主页、邀请、动态中应用拉黑过滤。

## 阶段 8：前端小程序

1. 初始化 Taro。
2. 实现登录页。
3. 实现创建宠物页。
4. 实现附近页。
5. 实现宠物主页。
6. 实现发起邀请页。
7. 实现邀请列表页。
8. 实现社交圈页。
9. 实现发布动态页。
10. 实现我的页。
11. 实现隐私设置页。

## 阶段 9：管理后台

1. 初始化 React Admin。
2. 实现登录。
3. 实现用户管理。
4. 实现宠物管理。
5. 实现动态管理。
6. 实现评论管理。
7. 实现举报处理。

---

# 十八、测试要求

## 18.1 后端测试

至少测试：

1. 微信登录。
2. 创建宠物。
3. 编辑宠物权限。
4. 附近查询。
5. 发起邀请。
6. 接受邀请。
7. 发布动态。
8. 点赞事务。
9. 评论事务。
10. 拉黑过滤。

## 18.2 前端测试

至少测试：

1. 未登录跳登录。
2. 没有宠物跳创建宠物。
3. 位置授权失败展示空状态。
4. 附近列表分页。
5. 邀请提交防重复。
6. 发布动态图片限制。
7. 隐私开关生效。

---

# 十九、最终交付物

Codex 最终需要生成：

```text
完整后端 Go Gin 项目
完整前端 Taro 小程序项目
完整管理后台 React 项目
数据库 migration
Docker Compose
README 启动文档
.env.example
API 文档
基础测试用例
```
